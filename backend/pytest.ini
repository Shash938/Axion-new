[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*

# Show locals on failure, verbose output
addopts = -v --tb=short

# Asyncio mode for future async tests
asyncio_mode = auto
