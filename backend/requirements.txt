# ==============================================================================
# AI Investment Advisor — Backend Dependencies
# ==============================================================================
# Use `pip install -r requirements.txt` to install.
# Intentionally uses compatible-release (~=) pins so pip can resolve
# pre-built wheels on all platforms (Windows, macOS, Linux).
# Lock exact versions for production using `pip freeze > requirements.lock`.
# ==============================================================================

# --- Web Framework ---
fastapi~=0.111.0
uvicorn[standard]~=0.29.0

# --- Data Validation & Settings ---
pydantic~=2.7.1
pydantic-settings~=2.2.1
python-dotenv~=1.0.1

# --- Financial Data ---
yfinance~=0.2.40

# --- Data Processing ---
pandas>=2.2.0
numpy>=1.24.0

# --- HTTP Client ---
httpx~=0.27.0

# --- Utilities & Security ---
python-multipart~=0.0.9
PyJWT>=2.0.0

# --- Optional: Face Recognition & WebAuthn ---
# These are optional; the app degrades gracefully if missing.
opencv-python-headless>=4.8.0
webauthn>=2.0.0

# --- Testing ---
pytest>=8.0.0
pytest-asyncio>=0.23.0
